<div class="row justify-content-center">

    <div class="col-xl-10 col-lg-12 col-md-9">

        <div class="card o-hidden border-0 shadow-lg my-5">
            <!-- Nested Row within Card Body -->
            <div class="col-lg-6">
                <div class="p-5">
                    <div class="text-center">
                        <h1 class="h4 text-gray-900 mb-4">이메일 인증</h1>
                        <h6><span id="emailSpan">{{email}}</span> 로 전송된 토큰번호를 입력해주세요</h6>

                        <hr/>
                    </div>
                    <form class="user">

<!--                        &lt;!&ndash;이메일&ndash;&gt;-->
<!--                        <div class="form-group">-->
<!--                            <input type="text" class="form-control form-control-user"-->
<!--                                   id="email" name="email" placeholder="{{email}}" readonly disabled/>-->
<!--                        </div>-->

                        <!--토큰-->
                        <div class="form-group">
                            <input type="text" class="form-control form-control-user"
                                   id="secretToken" name="secretToken" placeholder="토큰" autoComplete="off"/>
                            <span class="error-message" id="secretTokenErr"></span>
                        </div>

                        <button id="verifyBtn" type="button" class="btn btn-primary btn-user btn-block" >
                            인증하기
                        </button>

                        <hr/>
                        <div class="text-center">
                            <a class="small" href="#" id="tokenResendLink">토큰 재전송</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>